<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BaseProject" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="fontFamily">@font/nunito_sans_regular</item>
    </style>

    <style name="Theme.BaseProject" parent="Base.Theme.BaseProject" />
    <style name="BottomNavigationTextStyle" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textSize">10sp</item>
        <item name="android:paddingTop">8dp</item>
    </style>
    <style name="CustomBottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="android:minHeight">48dp</item> <!-- Đặt chiều cao tối thiểu -->
        <item name="itemIconSize">22dp</item> <!-- Kích thước icon nhỏ hơn -->
        <item name="itemPaddingTop">4dp</item> <!-- Padding trên nhỏ hơn -->
        <item name="itemPaddingBottom">4dp</item> <!-- Padding dưới nhỏ hơn -->
    </style>
</resources>